import validate from './validate';

describe('Validate should validate object against required rules', () => {

    const requiredRules = {
        id: false,
        first: true,
        last: true,
        age: true,
        description: true,
        thumbnail: false
    };

    it('', () => { 
        
    });

});
